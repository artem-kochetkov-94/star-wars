import styled from "styled-components";

const Label = styled.div`
  margin-bottom: 5px;
`;

const Error = styled.div`
  padding-top: 5px;
  color: red;
`;

export const Styled = {
  Label,
  Error,
};
